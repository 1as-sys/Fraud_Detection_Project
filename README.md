# Fraud Detection Project

## Objective
Detect fraudulent transactions using machine learning

## Tech Stack
Python, Pandas, Scikit-learn, Random Forest, Streamlit

## Project Structure
- `data/creditcard.csv` → Dataset
- `src/train_model.py` → Train ML models
- `src/app.py` → Streamlit app
- `models/rf_model.pkl` → Saved Random Forest model

## Steps to Run
1. Install dependencies:
```bash
pip install -r requirements.txt

